# ========== SentinelServer_AI 배포 환경변수 ==========
# 서비스 도메인
DOMAIN=bobsentinel.site
WWW_DOMAIN=www.bobsentinel.site

SERVER_IP=49.50.129.192

# 서비스 런타임 유저/그룹 (서버 환경에 맞게 변경)
RUN_USER=root
RUN_GROUP=root

# 앱 설치(작업) 경로 — 레포가 위치할 실제 경로
APP_DST=/root/SentinelServer_AI

MODEL_DIR=/root/runs/qwen4b_sft_merged2
MAX_NEW_TOKENS=256
USE_AI_DETECTOR=true

# 시스템 타임존
TZ_NAME=Asia/Seoul

# (선택) 대시보드 요약 API 보호용 키 — 쓰지 않으면 무인증
DASHBOARD_API_KEY=admin@@@
